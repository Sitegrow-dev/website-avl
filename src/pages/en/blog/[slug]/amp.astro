---
// AMP variant for /en/blog/[slug]/ : currently INACTIVE (FR AMP only).
import type { Post } from '@/data/posts';

export function getStaticPaths() {
  return [] as { params: { slug: string }; props: { post: Post } }[];
}

return Astro.redirect('/404/');
---
